#!/usr/bin/env python

import sys
import os.path
sys.path.insert(0, os.path.join(__file__, '..', 'lib'))

from tale import TaleService
from planes.lazy import serve, HostService
serve(HostService(TaleService()), port = 2380)

