#!/usr/bin/env python

import sys
import os.path
sys.path.insert(0, os.path.join(*(
    os.path.split(__file__)[:-1] +
    ('..', 'lib')
)))

from tale import TaleService
from planes.lazy import serve, StandardService, PathService
serve(StandardService(PathService(paths = {'session': TaleService()})), port = 2380)

